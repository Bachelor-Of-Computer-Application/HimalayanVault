<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>

<StackPane fx:id="rootPane"
           xmlns="http://javafx.com/javafx/21"
           xmlns:fx="http://javafx.com/fxml"
           fx:controller="com.himalayanvault.ui.LoginController"
           styleClass="root-pane">

    <!-- BACKGROUND IMAGE -->
    <ImageView fitHeight="${rootPane.height}" fitWidth="${rootPane.width}" preserveRatio="false">
        <image>
            <Image url="@../images/background_image.png"/>
        </image>
    </ImageView>

    <!-- BACKGROUND GRADIENT OVERLAY + CONTENT -->
        <VBox alignment="TOP_CENTER"
            spacing="0"
            styleClass="bg-pane">

        <!-- TOP LOGO HEADER -->
          <HBox fx:id="headerSection"
              alignment="CENTER_LEFT"
              spacing="12"
              styleClass="header-section"
              prefHeight="80"
              VBox.vgrow="NEVER">

            <HBox.margin>
                <Insets top="20" left="40" right="40"/>
            </HBox.margin>

            <ImageView styleClass="logo-icon" fitHeight="50" fitWidth="50" preserveRatio="true">
                <image>
                    <Image url="@../images/icon.png"/>
                </image>
            </ImageView>

            <VBox spacing="2">
                <Label text="HIMALAYAN"
                       styleClass="logo-text-main"/>
                <Label text="VAULT"
                       styleClass="logo-text-sub"/>
            </VBox>
        </HBox>

          <!-- CENTER CONTENT - Form -->
          <VBox fx:id="formWrapper"
              alignment="BOTTOM_CENTER"
              spacing="10"
              styleClass="form-wrapper"
              VBox.vgrow="ALWAYS"
              style="-fx-padding: 8 0 12 0;">

            <!-- TITLE -->
                 <Label fx:id="formTitle"
                     text="Login"
                     styleClass="form-title"/>

            <!-- WARNING BOX -->
            <HBox fx:id="warnBox"
                  styleClass="warn-box"
                  managed="false"
                  visible="false"
                  alignment="CENTER_LEFT"
                  spacing="8">
                <Label fx:id="warnLabel"
                       text=""
                       styleClass="warn-label"
                       wrapText="true"
                       HBox.hgrow="ALWAYS"/>
            </HBox>

            <!-- USERNAME FIELD -->
            <TextField fx:id="usernameField"
                       promptText="Enter your username"
                       styleClass="form-input-field"/>

            <!-- PASSWORD FIELD -->
            <PasswordField fx:id="passwordField"
                           promptText="Enter your password"
                           styleClass="form-input-field"
                           onAction="#handleUnlock"/>

            <!-- VISIBLE PASSWORD FIELD (Hidden by default) -->
            <TextField fx:id="visibleField"
                       promptText="Enter your password"
                       styleClass="form-input-field"
                       managed="false"
                       visible="false"/>

            <!-- BUTTONS ROW -->
            <HBox fx:id="buttonContainer"
                alignment="CENTER"
                spacing="12"
                styleClass="button-container">

                <!-- LOGIN BUTTON -->
                <Button fx:id="unlockButton"
                    text="LOG IN"
                    styleClass="primary-button"
                    onAction="#handleUnlock"
                    prefHeight="32"
                    HBox.hgrow="ALWAYS"/>

                <!-- BIOMETRIC BUTTON -->
                <Button fx:id="eyeButton"
                    onAction="#handleBiometric"
                    text="BIO"
                        styleClass="biometric-button"
                        prefWidth="34"
                    prefHeight="32"/>
            </HBox>

            <!-- LINKS ROW -->
            <HBox fx:id="linksContainer"
                alignment="CENTER"
                                    spacing="16"
                styleClass="links-container">

                <Hyperlink text="Forgot Password"
                           styleClass="form-link"
                           onAction="#handleRecovery"/>

                <Label text="|"
                       styleClass="link-separator"/>

                <Hyperlink text="Sign Up"
                           styleClass="form-link"
                           onAction="#navigateToSignup"/>
            </HBox>

        </VBox>

    </VBox>

</StackPane>